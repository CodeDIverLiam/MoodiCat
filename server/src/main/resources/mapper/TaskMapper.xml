<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aidiary.mapper.TaskMapper">
    <select id="findById" resultType="com.aidiary.model.Task">
        SELECT id, user_id as userId, title, description, status, due_date as dueDate, created_at as createdAt, updated_at as updatedAt 
        FROM tasks WHERE id = #{id}
    </select>

    <select id="findByUserId" resultType="com.aidiary.model.Task">
        SELECT id, user_id as userId, title, description, status, due_date as dueDate, created_at as createdAt, updated_at as updatedAt 
        FROM tasks
        WHERE user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tasks (user_id, title, description, status, due_date)
        VALUES (#{userId}, #{title}, #{description}, #{status}, #{dueDate})
    </insert>

    <update id="update">
        UPDATE tasks
        SET title = #{title}, description = #{description}, status = #{status}, due_date = #{dueDate}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM tasks WHERE id = #{id}
    </delete>

    <select id="findByUserIdAndDate" resultType="com.aidiary.model.Task">
        SELECT id, user_id as userId, title, description, status, due_date as dueDate, created_at as createdAt, updated_at as updatedAt 
        FROM tasks WHERE user_id = #{userId} AND (created_at BETWEEN #{date} AND #{date} + INTERVAL 1 DAY)
    </select>
</mapper>
